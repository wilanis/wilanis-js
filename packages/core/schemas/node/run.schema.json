{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/wilanis/wilanis-js/main/packages/core/schemas/node/run.schema.json",
  "title": "node/run",
  "description": "Run one operation. in gives every value it takes: a literal, or {{node.path}} / {{in.path}} / {{const.name}} / {{resolver}} to read one. Runs the moment every value it reads has settled.",
  "type": "object",
  "properties": {
    "type": {
      "description": "Marks this node a run: it calls one operation.",
      "const": "@wilanis/node/run.schema.json"
    },
    "id": {
      "$ref": "../common.schema.json#/$defs/ident"
    },
    "description": {
      "description": "What this node does, for a reader of the graph.",
      "type": "string"
    },
    "label": {
      "description": "A short human name for this node, shown wherever a reader sees it instead of its id.",
      "type": "string",
      "minLength": 1
    },
    "run": {
      "$ref": "../common.schema.json#/$defs/opRef"
    },
    "in": {
      "$ref": "../common.schema.json#/$defs/values"
    },
    "timeoutMs": {
      "$ref": "../common.schema.json#/$defs/timeoutMs"
    },
    "retry": {
      "$ref": "../common.schema.json#/$defs/retry"
    }
  },
  "required": [
    "type",
    "id",
    "run"
  ],
  "additionalProperties": false
}
