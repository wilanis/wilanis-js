{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/wilanis/wilanis-js/main/packages/core/schemas/connection-kind.schema.json",
  "title": "connection-kind",
  "description": "A plugin-shipped connection contract: the settings a connection of this kind carries.",
  "type": "object",
  "properties": {
    "$schema": {
      "description": "The kind of this document: its published schema URL, or the alias @wilanis/connection-kind.schema.json.",
      "enum": [
        "https://raw.githubusercontent.com/wilanis/wilanis-js/main/packages/core/schemas/connection-kind.schema.json",
        "@wilanis/connection-kind.schema.json"
      ]
    },
    "description": {
      "description": "What this kind of connection reaches, in one or two sentences.",
      "type": "string",
      "minLength": 1
    },
    "label": {
      "description": "A short human name for this document, shown wherever a reader sees it instead of its path. Absent: the file name stands in.",
      "type": "string",
      "minLength": 1
    },
    "settings": {
      "$ref": "common.schema.json#/$defs/inlineObject"
    },
    "storage": {
      "description": "true: a connection of this kind reaches a storage engine, so an @storage store may name it. The plugin that grants the kind registers the engine behind it.",
      "type": "boolean"
    },
    "leases": {
      "description": "true: a connection of this kind can keep a lease -- a named hold one process of several takes for a while, and the record of what was last done under it. The plugin that grants the kind registers a lease keeper for it; the scheduler's `run` step names such a connection as its `lease` (X254). Absent: a connection of this kind keeps no lease (an HTTP upstream, a directory).",
      "type": "boolean"
    }
  },
  "required": [
    "$schema",
    "description",
    "settings"
  ],
  "additionalProperties": false
}
