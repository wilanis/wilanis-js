{
  "$schema": "https://raw.githubusercontent.com/wilanis/wilanis-js/main/packages/core/schemas/connection.schema.json",
  "label": "Monitor API",
  "description": "The monitor's REST API: GET /customers, GET /customers/{id}, POST /customers, PUT /customers/{id}, DELETE /customers/{id}. A missing id answers 404 with the JSON string \"Not found\". At most four requests are in flight against it at once: a batch removal issues one DELETE per id, and the throttle paces them.",
  "kind": "@http/http.connection-kind.json",
  "settings": {
    "baseUrl": "https://6aa009e23e0d88d3d7e5525d.mockapi.io/api/v1",
    "timeoutMs": 10000,
    "throttle": {
      "concurrency": 4
    }
  }
}
