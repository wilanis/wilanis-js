{
  "$schema": "https://raw.githubusercontent.com/wilanis/wilanis-js/main/packages/core/schemas/connection.schema.json",
  "label": "Jobs",
  "description": "The broker the customers' queued work goes through: the in-process broker, whose queues live as long as the process that published to them, so a message is consumed by the process whose route put it there. It delivers at least once, as a broker in production will. Once the storage engine grants a broker, production names that kind instead, and the queue is a table beside the customers.",
  "kind": "@queue-memory/memory.connection-kind.json",
  "settings": {}
}
