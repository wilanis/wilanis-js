{
  "$schema": "https://raw.githubusercontent.com/wilanis/wilanis-js/main/packages/core/schemas/graph.schema.json",
  "label": "A key for a new customer",
  "description": "Data graph behind customer.nextId under the local profile: a key no customer of the store has, asked for before the customer is made, so the domain holds the whole record before anything is written.",
  "out": {
    "type": "string",
    "from": "key"
  },
  "nodes": [
    {
      "type": "@wilanis/node/run.schema.json",
      "id": "key",
      "label": "A key no record has",
      "run": "@storage/store.port.json#newKey",
      "in": {
        "store": "@customers/data/customers.store.json",
        "collection": "customers"
      }
    }
  ]
}
