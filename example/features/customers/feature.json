{
  "$schema": "https://raw.githubusercontent.com/wilanis/wilanis-js/main/packages/core/schemas/feature.schema.json",
  "label": "Customers",
  "description": "What the registry keeps: list them, read one, register one, update one, remove one, a digest, and a CSV import and export. Every customer is kept under the tenant of whoever wrote it; a read is for a signed-in caller and answers their tenant's customers, the digest reads every tenant's for an employee, and every write is for a signed-in employee holding the registrar role.",
  "exports": [
    "@features/customers/domain/customer.port.json",
    "@features/customers/domain/Customer.shape.json"
  ],
  "effects": [
    "@http/http.port.json#request",
    "@blob/csv.port.json#parse",
    "@blob/csv.port.json#write",
    "@storage/store.port.json#find",
    "@storage/store.port.json#get",
    "@storage/store.port.json#put",
    "@storage/store.port.json#patch",
    "@storage/store.port.json#remove",
    "@storage/store.port.json#newKey",
    "@storage/storage.port.json#ensure"
  ],
  "dependsOn": [
    "access"
  ]
}
