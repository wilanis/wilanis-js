{
  "$schema": "https://raw.githubusercontent.com/wilanis/wilanis-js/main/packages/core/schemas/shape.schema.json",
  "label": "Update body",
  "layer": "edge",
  "description": "PUT /customers/{id} body: who the customer now is. The id is the route's, never the body's, so a client cannot move a customer by sending another one. Closed: an undeclared field is a 400.",
  "fields": {
    "name": {
      "type": "string"
    },
    "email": {
      "type": "string"
    },
    "tier": {
      "type": "string",
      "enum": ["bronze", "silver", "gold"]
    },
    "note": {
      "type": "string",
      "required": false,
      "description": "why the customer is of their tier; a gold customer carries one"
    }
  }
}
