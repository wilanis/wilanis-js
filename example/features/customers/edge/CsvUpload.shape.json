{
  "$schema": "https://raw.githubusercontent.com/wilanis/wilanis-js/main/packages/core/schemas/shape.schema.json",
  "label": "CSV upload",
  "layer": "edge",
  "description": "What POST /customers.csv hands the domain: the uploaded file, as its handle in the blob registry.",
  "fields": {
    "file": {
      "type": "blob",
      "description": "the CSV file"
    }
  }
}
