{
  "$schema": "https://raw.githubusercontent.com/wilanis/wilanis-js/main/packages/core/schemas/shape.schema.json",
  "label": "Update request",
  "layer": "edge",
  "description": "What PUT /customers/{id} hands the domain: the id from the route, and who the customer now is from the body.",
  "fields": {
    "id": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "email": {
      "type": "string"
    },
    "tier": {
      "type": "string",
      "enum": ["bronze", "silver", "gold"]
    },
    "note": {
      "type": "string",
      "required": false,
      "description": "why the customer is of their tier; a gold customer carries one"
    }
  }
}
