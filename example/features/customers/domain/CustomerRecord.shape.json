{
  "$schema": "https://raw.githubusercontent.com/wilanis/wilanis-js/main/packages/core/schemas/shape.schema.json",
  "label": "Customer record",
  "layer": "core",
  "description": "A customer as the customer API is sent one when it creates the row: who they are and who registered them, without the key, which the API gives the row itself.",
  "fields": {
    "name": {
      "type": "string"
    },
    "email": {
      "type": "string"
    },
    "tier": {
      "type": "string"
    },
    "registrar": {
      "type": "string",
      "required": false,
      "description": "who registered this customer"
    }
  }
}
