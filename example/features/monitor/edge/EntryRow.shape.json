{
  "$schema": "https://raw.githubusercontent.com/wilanis/wilanis-js/main/packages/core/schemas/shape.schema.json",
  "label": "API row",
  "layer": "edge",
  "description": "A record as the monitor API returns it. Extra fields are tolerated -- reponseStatus is the API's own spelling and stays at the edge; trigger-facing shapes never see it.",
  "fields": {
    "id": {
      "type": "string"
    },
    "url": {
      "type": "string"
    },
    "method": {
      "type": "string"
    },
    "reponseStatus": {
      "type": "number",
      "required": false
    },
    "ipv4": {
      "type": "string",
      "required": false
    },
    "mac": {
      "type": "string",
      "required": false
    },
    "agent": {
      "type": "string",
      "required": false
    },
    "note": {
      "type": "string",
      "required": false
    },
    "createdAt": {
      "type": "string",
      "required": false
    }
  },
  "open": true
}
