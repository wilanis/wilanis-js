{
  "$schema": "https://raw.githubusercontent.com/wilanis/wilanis-js/main/packages/core/schemas/shape.schema.json",
  "label": "Latest call of a method",
  "layer": "core",
  "description": "The most recent call recorded for one HTTP method: which method, where it went, and the entry that recorded it. One record per method, so the method is the key.",
  "fields": {
    "method": {
      "type": "string",
      "description": "the HTTP method, which is also what identifies the record"
    },
    "url": {
      "type": "string",
      "description": "the URL of the most recent call recorded for it"
    },
    "entry": {
      "type": "string",
      "description": "the id of the entry that recorded it"
    }
  }
}
