{
  "$schema": "https://raw.githubusercontent.com/wilanis/wilanis-js/main/packages/core/schemas/shape.schema.json",
  "label": "Entry",
  "layer": "core",
  "description": "An observed HTTP call as the domain knows it.",
  "fields": {
    "id": {
      "type": "string"
    },
    "url": {
      "type": "string"
    },
    "method": {
      "type": "string"
    },
    "agent": {
      "type": "string",
      "required": false,
      "description": "the user agent that made the call"
    },
    "note": {
      "type": "string",
      "required": false,
      "description": "what a reader wrote about this call; a note lives on the entry itself"
    }
  }
}
