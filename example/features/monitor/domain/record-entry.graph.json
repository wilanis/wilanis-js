{
  "$schema": "https://raw.githubusercontent.com/wilanis/wilanis-js/main/packages/core/schemas/graph.schema.json",
  "label": "Record an entry",
  "description": "Domain graph behind POST /monitor: every entry recorded through this API is attributed to this recorder; the domain decides that, not the client.",
  "constants": {
    "recorder": {
      "type": "string",
      "value": "wilanis-example/0.1.0",
      "description": "the user agent every recorded entry carries"
    }
  },
  "in": "@monitor/domain/EntryDraft.shape.json",
  "out": {
    "type": "@monitor/domain/Entry.shape.json",
    "from": "recorded"
  },
  "nodes": [
    {
      "type": "@wilanis/node/run.schema.json",
      "id": "recorded",
      "label": "Store it",
      "run": "@monitor/domain/monitor.port.json#record",
      "in": {
        "url": "{{in.url}}",
        "method": "{{in.method}}",
        "agent": "{{const.recorder}}"
      }
    }
  ]
}
