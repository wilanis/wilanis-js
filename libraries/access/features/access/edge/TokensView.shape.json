{
  "$schema": "https://raw.githubusercontent.com/wilanis/wilanis-js/main/packages/core/schemas/shape.schema.json",
  "label": "Tokens (answer)",
  "description": "What a sign-in or a refresh answers the caller: the access token to present as Authorization: Bearer (also set as the session cookie), the refresh token to renew it with, and when it expires.",
  "layer": "edge",
  "fields": {
    "accessToken": {
      "type": "string",
      "secret": true
    },
    "refreshToken": {
      "type": "string",
      "secret": true
    },
    "tokenType": {
      "type": "string"
    },
    "expiresIn": {
      "type": "number"
    }
  }
}
