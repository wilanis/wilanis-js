{
  "$schema": "https://raw.githubusercontent.com/wilanis/wilanis-js/main/packages/core/schemas/trigger.schema.json",
  "label": "POST /customers/{id}/archive",
  "description": "Pasted over features/customers/edge/archive-customer.trigger.json in beat 3 of docs/demo.md: the route with its shapes and refusals filled in as the T002 and T005 hints said, and still no policy, so one refusal is left, I001.",
  "kind": "@http/http.trigger-kind.json",
  "settings": {
    "route": "/customers/{id}/archive",
    "method": "POST",
    "produces": "application/json",
    "response": {
      "refusals": {
        "missing": 404,
        "upstream": 502,
        "invariant": 500
      }
    }
  },
  "in": "@customers/edge/IdRequest.shape.json",
  "out": "@customers/edge/CustomerView.shape.json",
  "fire": {
    "run": "@customers/domain/customer.port.json#remove",
    "in": {
      "id": "{{request.params.id}}"
    }
  }
}
